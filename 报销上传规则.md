# 报销上传规则

> Agent 执行图灵 ERP 报销凭证上传的**操作指令集**。命名、匹配、供应商映射的权威规则以以下文件为准，本文只保留图灵上传操作步骤与入口，避免多处复述导致不一致。

---

## 1. 文件位置
- 支付凭证文件夹：`D:\ai共享盘\MyBrain\支付凭证`
- 知识库路径：`D:\ai共享盘\MyBrain`

## 2. 权威规则入口（先读这些，再上传）
- [[数据/凭证命名规则|凭证命名规则]] — 凭证图片命名、合并、金额校验的唯一权威
- [[数据/供应商列表|供应商列表]] — 供应商标准名称、凭证关键词、易混供应商
- [[AI执行指南/凭证匹配决策表|凭证匹配决策表]] — 金额/供应商匹配判断流程
- [[AI执行指南/异常处理决策表|异常处理决策表]] — 异常应对策略

> 命名格式（如 `金额-供应商全称.jpg`、同金额同供应商第二张用 `金额-2-供应商全称.jpg`）、合并规则、金额校验等**全部以 [[数据/凭证命名规则|凭证命名规则]] 为准**，本文不再复述。

## 3. 图灵上传操作步骤（本文独有）
1. 点击该行「上传凭证」按钮
2. 按 `Alt+D` 定位地址栏
3. 粘贴文件夹路径 `D:\ai共享盘\MyBrain\支付凭证` 后回车
4. 按 `Tab` 切换到文件列表
5. 用方向键选中匹配的文件
6. 按 `Enter` 确认上传

## 4. 完整指令（复制给 Agent）
> 参考我的知识库「报销上传规则」，在图灵系统当前页面，自动匹配并上传支付凭证。完成后告诉我结果。

## 5. 异常处理
同金额同供应商、找不到文件、供应商关键词不匹配、上传后无成功反馈等情况，按 [[AI执行指南/异常处理决策表|异常处理决策表]] 处理。
