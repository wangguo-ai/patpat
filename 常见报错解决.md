# 常见报错解决

> Agent 执行报销上传/采购入库时可能遇到的问题及解决方式。

---

## Git / Obsidian 同步

| 报错 | 原因 | 解决 |
|------|------|------|
| `invalid gitfile format: D:\ai\Git\cmd\git.exe` | Obsidian Git 插件的 gitDir 填了 exe 完整路径 | 清空 gitDir 或改为 `D:\ai\Git\cmd` |
| `Can't find a valid git repository` | 父级目录有空的 .git 干扰 | 删除父级空 .git，确保仓库 .git 在库根目录 |
| push 无响应 | 未授权 GitHub | Obsidian 内 Ctrl+P → Git: Push → 浏览器弹窗授权 |

---

## 凭证匹配

| 报错 | 原因 | 解决 |
|------|------|------|
| 找不到凭证文件 | 文件名不匹配，或凭证未放入支付凭证目录 | 检查 `D:\ai共享盘\MyBrain\支付凭证` 目录，确认命名规则 |
| 同金额同供应商 | 可能是独立凭证 | 跳过该条，汇总时标记人工判断 |
| 广信/广盛混淆 | 两个供应商名称相近 | 停止匹配，人工确认后继续 |

---

## 影刀 RPA

| 报错 | 原因 | 解决 |
|------|------|------|
| 识别失败不停止 | 影刀流程未配置失败中断 | 修改影刀流程，OCR 失败时中断 |
| 一次性场景无法重跑 | 数据已被处理 | 恢复备份文件后重跑 |
| 重复凭证合并不稳定 | 合并逻辑不完善 | 改用 AI 预处理生成命名，影刀只做上传 |

---

## 图灵 ERP

| 报错 | 原因 | 解决 |
|------|------|------|
| 搜索不到供应商 | 名称不匹配或ID错误 | 用[[数据/供应商列表|供应商列表]]中的标准名称 |
| 上传后无反馈 | 网络延迟或弹窗被拦截 | 等待3秒刷新页面确认 |
| 采购单号找不到 | 单号不在当前页面 | 检查是否在正确的板块（版聊采购/待收货） |

---

## Agent 自查流程

1. 检查是否已读取 [[AI执行指南/Agent读取顺序|Agent读取顺序]]
2. 检查凭证目录 `D:\ai共享盘\MyBrain\支付凭证` 是否存在
3. 检查当前任务文件是否是最新版本
4. 如遇异常，先查本表再查[[AI执行指南/异常处理决策表|异常处理决策表]]

---

## 关联笔记
- [[报销上传规则]]
- [[AI执行指南/异常处理决策表|异常处理决策表]]
- [[系统/影刀RPA|影刀RPA]]
- [[系统/图灵ERP|图灵ERP]]
