# 常见报错解决

> Agent 执行报销上传/采购入库时可能遇到的问题及解决方式。

---

## Git / Obsidian 同步（本库独有）

| 报错 | 原因 | 解决 |
|------|------|------|
| `invalid gitfile format: D:\ai\Git\cmd\git.exe` | Obsidian Git 插件的 gitDir 填了 exe 完整路径 | 清空 gitDir 或改为 `D:\ai\Git\cmd` |
| `Can't find a valid git repository` | 父级目录有空的 .git 干扰 | 删除父级空 .git，确保仓库 .git 在库根目录 |
| push 无响应 | 未授权 GitHub | Obsidian 内 Ctrl+P → Git: Push → 浏览器弹窗授权 |

---

## 业务类报错（按场景查对应权威文档）

业务执行中的报错（凭证匹配、影刀 RPA、图灵 ERP 等）不要在此表硬查，统一按异常类型查权威文档：

- **异常分类与应对策略**：[[AI执行指南/异常处理决策表|异常处理决策表]]
- **图灵 ERP 操作报错**：[[系统/图灵ERP|图灵ERP]] → 操作注意事项
- **影刀 RPA 报错**：[[系统/影刀RPA|影刀RPA]] → 已知问题与注意事项

常见高频异常速查：

| 现象 | 权威出处 | 一句话处理 |
|------|----------|------------|
| 同金额同供应商 | 异常处理决策表 | 跳过该条，汇总时标记人工判断 |
| 广信/广盛混淆 | 异常处理决策表 | 停止匹配，人工确认后继续 |
| 找不到凭证文件 | 异常处理决策表 / 凭证命名规则 | 检查支付凭证目录与命名规则 |
| 上传后无反馈 | 异常处理决策表 | 等待3秒刷新页面确认，重试一次 |
| 搜索不到供应商 | 图灵ERP | 用供应商列表中的标准名称 |
| 影刀识别失败不停止 | 影刀RPA | 修改影刀流程，OCR 失败时中断 |

---

## Agent 自查流程

1. 检查是否已读取 [[AI执行指南/Agent读取顺序|Agent读取顺序]]
2. 检查凭证目录 `D:\ai共享盘\MyBrain\支付凭证` 是否存在
3. 检查当前任务文件是否是最新版本
4. 如遇异常，先查 [[AI执行指南/异常处理决策表|异常处理决策表]]，再查对应系统文档
