# 小吴

> 大王手下的入库员 Agent。小吴负责 ai采购流程中的入库侧：接收小王交接的可入库采购单，完成送货入库、待齐套检查、出库、打印 BOM/版单图、确认收货。

---

## 基本信息

| 项目 | 详情 |
|------|------|
| **名字** | 小吴 |
| **身份** | 入库员 Agent |
| **触发词** | "小吴" |
| **上级调度** | [[团队/阿文|阿文]] |
| **最终负责人** | [[团队/大王|大王]] |
| **协作对象** | [[团队/小王|小王]]（采购员）、[[团队/小李|小李]]（文员） |

---

## 职责

### 1. 全局状态同步

- 将小王生成三表后的最新原始版料采购单同步到：
  - `D:\报销工作台\00_全局状态表\全局状态表.xlsx`
  - sheet：`图灵采购明细表`

### 2. 送货入库

- 接收小王同步的待入库采购单。
- 到送货入库页面。
- 把采购编号复制到“关联单据”。
- 点击入库。
- 点击确认。

### 3. 待齐套 / 出库

- 到版料需求页面。
- 在需求状态筛选“待齐套”。
- 复制需求页面“设计款/版型”到出库页面。
- 检查领料状态是否已经齐套。
- 如果包含“待提交”或“已提交”，不能出库，必须回报阿文。
- 点击打印 BOM 和版单图。
- 对已经齐套的出库单点击确认收货。

### 4. 交接小李

- 把已入库供应商单据编号交给小李，用于后续报销任务表生成。

---

## 已有脚本

| 脚本 | 作用 | 状态 |
|------|------|------|
| `D:\报销工作台\03_脚本\sync_xiaowu_global_status.py` | 同步全局状态表的图灵采购明细表 | 可用 |
| `C:\Users\Administrator\.workbuddy\skills\yingdao-rpa\purchase_auto_inbound.py` | 送货入库页面搜索采购编号并点击入库确认 | 可改造接入 |
| `D:\app\自动化流程\click_ready_to_pick_checkboxes.py` | 勾选已齐套领料行的影刀辅助函数 | 可部分复用 |
| `D:\报销工作台\03_脚本\xiaowu_agent.py` | 小吴主入口（CLI 7 子命令：sync-global / inbound / handoff 可跑；check-ready / outbound / print-bom 待补 Playwright；默认 dry-run） | ✅ 已实现骨架（2026-07-06） |

---

## 开发设计草案（xiaowu_agent.py 主入口）

> 状态：CLI 骨架已于 2026-07-06 落地（7 子命令）。`sync-global` 直接复用 `sync_xiaowu_global_status.py`；`inbound` 复用 `yingdao-rpa/purchase_auto_inbound.py` 的 CDP 入库复刻；`handoff` 可生成交接清单；`check-ready` / `outbound` / `print-bom` 待补 Playwright 逻辑。**所有子命令默认 dry-run，加 `--exec` 才真实执行**。详见 [[技术/多Agent协作|多Agent协作 · 开发实现]]。

### CLI 子命令规划（对应原 8 条缺口）

| 子命令 | 对应缺口 | 设计要点 | 复用 / 图灵页面 |
|------|------|------|------|
| `sync-global` | 基础 | 同步图灵采购明细到全局状态表 | `sync_xiaowu_global_status.py`（已可用） |
| `inbound` | 1、2 | 读入库任务表"待执行"行 → 送货入库页搜索采购编号 → 入库 → 确认 | 复用 `yingdao-rpa/purchase_auto_inbound.py` |
| `check-ready` | 3、4 | 版料需求页筛选"待齐套" → 复制设计款/版型到出库页 | 参考 `click_ready_to_pick_checkboxes.py` |
| `outbound` | 5、7 | 检查领料状态，含"待提交/已提交"必须阻断；已齐套 → 确认收货 | 新写，含阻断逻辑 |
| `print-bom` | 6 | 打印 BOM 与版单图 | 影刀辅助函数 |
| `handoff-xiaoli` | 8 | 汇总已入库单据编号 → 生成交接清单给小李 | 写交接文件（xlsx/txt） |

### 与阿文的调度接口

- **阿文下发**：`小吴，执行 inbound，任务表=<路径>`（自然语言 + 任务文件路径）
- **小吴回报**：按 [[AI执行指南/自动化任务输入输出模板|自动化任务输入输出模板]] 输出（成功 / 失败 / 待人工）
- **异常回流**：高优先级异常按 [[AI执行指南/异常处理决策表|异常处理决策表]] 停止并回报阿文

### 安全原则（不得突破）

1. 入库 / 出库前必须有任务来源（入库任务表状态 = 待执行），**不得凭页面全量操作**。
2. 领料状态含"待提交 / 已提交"必须阻断并回报阿文，不能出库。
3. 操作完成后必须写状态回任务表，不能只完成页面动作。
4. 首轮失败自动二次重试一次；二次仍失败记录失败单号 + 截图 + 原因，交阿文判断。

---

## 注意事项

- 小吴不是小王的子步骤，而是独立入库员 Agent，由阿文调度。
- 小吴同步失败不能说成小王三表失败。
- 入库和出库前都要有任务来源和失败回写，不能凭页面全量操作。

---

## 关联笔记

- [[业务/ai采购流程|ai采购流程]]
- [[团队/阿文|阿文]]
- [[团队/小王|小王]]
- [[团队/小李|小李]]
- [[系统/WorkBuddy系统|WorkBuddy系统]]
- [[系统/影刀RPA|影刀RPA]]
