# 2026-07-07 工作日志

## 小吴 agent 三子命令补全（check-ready / outbound / print-bom）
- 文件：`D:\报销工作台\03_脚本\xiaowu_agent.py`
- 复用 `yingdao-rpa/purchase_auto_inbound.py` 的 `init_browser(cdp_port)` 连 CDP 已登录浏览器。
- 新增辅助：`connect_browser()` / `read_global_status()`（读全局状态表·图灵采购明细表）/ `pick_status_blocks()`（领料状态阻断判断）。
- `check-ready`：接通全局状态表数据层，dry 实测读 888 行、筛出 106 个宽松候选；页面层（版料需求页「待齐套」tab）仍 TODO。
- `outbound`：出库硬规则落地——领料状态含「待提交/已提交」必须阻断并回报阿文；`--demo-status` 验证阻断/放行均通过。
- `print-bom`：结构化（参数 `--order-nos`），页面打印交互 TODO。
- 三子命令默认 dry-run，加 `--exec` 才连浏览器；编译 + --help + 阻断演示验证通过，未触真实业务。

## 图灵页面 CDP 探查 + 三子命令页面层补全（2026-07-07 续）
- 大王开启本机 Chrome 远程调试端口（9222，独立 profile 登录图灵）。我连 CDP 只读探查（不点业务按钮）。
- 关键确认：图灵=`turing.patpat.shop`（patpat 公网 SaaS，非腾讯内网）；版料需求列表为 **vxe-table**；领料状态列≈td[9]、齐套情况位列≈td[10]；已定位版料需求页 URL（`ver-material-demand-list`）及多个出库候选页。
- 三个子命令页面交互层已补全真实 URL + vxe-table 选择器：`check-ready`/`outbound` 在 exec 模式连 CDP 读表数据，`print-bom` 结构就绪（打印按钮交互待 --exec 实测）。
- 兼容性坑（已全踩并解决）：① playwright `screenshot()` 在 Chrome149+CDP 段错误；② 大 `evaluate`（遍历 vxe-table tbody tr 返回大对象）也偶发段错误——**改用 `page.content()` 拿整页 HTML + Python 本地解析**（绕过浏览器端大操作），稳定；③ Windows GBK 终端不支持 emoji，原 `✅/⚠️/⛔` 崩溃，已改 ASCII 标记。
- 列结构精确确认（用 `colid` 属性映射，非位置猜）：版料需求列表 vxe-table 列以 `colid` 标识（col_7~col_25），前 3 列（col_7 复选框/col_8 序号/col_9 需求单号）为隐藏固定列；**真实列名「领料单状态=col_17」（非「领料状态」）、「齐套时间=col_21」（--=未齐套，有值=已齐套）；无「齐套情况位」列**；需求单号(col_9)在 HTML 文本为空，设计款/版型(col_10)为可用主键。
- exec 联调结果：用大王开端口时抓的真实存档 HTML（`MyBrain/tools/sample_demand_page.html`，1MB/20 行）跑 `--exec --html` 验证脚本解析逻辑 100% 正确——`check-ready` 正确标 15 阻断/5 放行、`YHC260700006` 未齐套；`outbound` 总20|阻断15|放行5，阻断清单精准命中所有「已提交」行。
- **实机 `--exec`（连 CDP）偶发段错误**：该 Chrome 实例被多次 CDP 连接后状态不稳（段错误不可捕获），非脚本逻辑错。重启调试窗口可恢复。已新增 `--html` 离线复核模式（共用同一解析代码），优先用 --html 复核。
- 临时探针脚本（probe_turing_cdp*.py / _exec_*.py）已清理；真实样本 HTML 保留为 `MyBrain/tools/sample_demand_page.html`。
- 文档已同步：`团队/小吴.md` 列名/列索引修正为真实值，注明 --html 模式与 CDP 段错误注意。

## 身份口径统一
- 大王确认以 `MyBrain` 知识库 .md 的「面料部主管（广州番禺）」为准。
- 修正：我的长期记忆快照曾把大王写成「邦正机械海外视频运营负责人」；已明确——邦正机械是另一独立业务线，勿与大王身份混淆。口径写入 `~/.workbuddy/MEMORY.md`。

## ⚠️ 图灵页面映射修正（2026-07-07 大王指出）
- **之前错误**：把入库/出库都建在「版料需求页」(ver-material-demand-list) 上。
- **正确映射**（均 `inventory-management42` 下）：
  - 入库(inbound) = `inWarehouse-manage`  ← 代码 TURING_INBOUND_URL 原本就对的
  - 出库(outbound) = `allocate-manage`    ← 代码原本错写成 `plate-material-management/outbound-manage`，已改正
  - 版料需求页(ver-material-demand-list) 仅用于 `check-ready` 齐套检查（读取/筛选），**不是**入库/出库操作页
- **影响**：`outbound` 此前按版料需求页解析的 `col_17=领料单状态 / col_21=齐套时间` 列名**不直接适用** `allocate-manage`；阻断逻辑 `pick_status_blocks()` 可复用，但「领料单状态」这一列名需在 allocate-manage 重新探查确认。
- **已落地**：`xiaowu_agent.py` 的 `TURING_OUTBOUND_URL` 已改为 allocate-manage；新增 `resolve_allocate_page()`；outbound exec 现跳 allocate-manage 并打印真实列名再判定。`check-ready` 仍用版料需求页（符合修正）。
- **待办**：重新探查 `allocate-manage` 的 vxe-table 列结构（需大王重开 CDP 或提供该页 HTML），据实际列名修正 outbound 的阻断列键（当前 15 BLOCK/5 OK 验证是基于需求页数据，对出库不具代表性）。`sample_demand_page.html` 是需求页样本，outbound --html 复核需换 allocate-manage 样本。
