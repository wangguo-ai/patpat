# 2026-07-08 工作日志

## 图灵入库只读验证（MCG202607070008）
- 大王请求"验证 MCG202607070008 能否入库"（明确只读、不点按钮，吸取 7-7 误操 MCG202607070003 的教训）。
- 端口重开（launch_chrome_turing_cdp.bat）。连 CDP 偶发段错误，重试 + 修 verify 脚本漏写的 `from playwright.async_api import async_playwright` 后连上。
- 诊断 `diag_tabs.py` 发现：当前 tab = `https://turing.patpat.shop/login`（**未登录**，提示"邮箱对应的用户不存在"），表格 th=0/tr=0，故搜不到任何单。
- 已写 `verify_inbound_readonly.py`（wait_ready 渲染轮询 + 校验安全网，全程只读不点按钮）与 `diag_tabs.py`。
- **阻塞**：需大王在 Chrome 窗口用正确账号密码登录图灵，并导航到入库管理页（inventory-management42/inWarehouse-manage，已渲染出 vxe-table）后，重跑验证。
- 技术坑复现：playwright `connect_over_cdp` 在本地 Chrome149 调试端口偶发段错误（整进程崩，Python 层捕获不到），重试通常能过；首屏表格需轮询等待渲染，不能只等固定时长。

## 图灵入库流程排查（大王给的正确步骤 + MCG202607070008 定位失败）
- 大王纠正正确入库步骤：入库页「展开」按钮 → 在「搜索关联单据」输入框输入单号 → 点「搜索」→ 点「入库」→ 弹窗确认米数 → 点「确认」。
- 实测排查（多次 CDP 连接重试，已连上大货 tab）：
  1. 顶部全局搜索框（placeholder="请输入"，label=入库申请单）填 MCG 后点搜索 → 空表。**该框只认「入库申请单」(MRK) 列，不认 MCG（关联单据列）**。
  2. 之前 `scYoq1` 报告的「搜 MCG 返回60行」是**假阳性**——那是未触发过滤的默认数据，并非搜索命中。
  3. 点「展开」后筛选区只有「入库申请单」输入框 + 两个"请选择"下拉，**未发现"关联单据"专门输入框**（大王描述的框未出现，疑点错展开按钮，或该框是「关联单据」列的列头筛选）。
  4. 切「大货」tab（MCG 单在此 tab）+ 清空搜索 + 滚动默认列表 30 次 → 仍**定位不到 MCG202607070008**。
- **结论**：MCG202607070008 在当前视图（大货 tab 默认）无法定位。可能：① 单号有误/不存在；② 状态被默认筛选排除（如非待入库）；③ "搜索关联单据"实为列头筛选，需大王确认 UI 位置。
- 关键认知：搜索框文字带空格（"搜 索"/"重 置"），JS 匹配必须去空格；按钮「入库」也带空格需去空格匹配。MCG 行的「入库申请单」(MRK) 列为空，是按采购单行直接入库。
- 临时脚本（D:\报销工作台\03_脚本\）：do_inbound_mcg.py（切大货tab+全局框搜+入库全流程）、diag_mcg.py（只读诊断单号存在性）、scroll_inbound.py（滚动定位入库，绕过搜索框）、verify_inbound_readonly.py、diag_tabs.py。
- **待大王确认**：MCG202607070008 单号是否准确；"展开"按钮与"搜索关联单据"输入框在页面确切位置（或截图圈出），以便固化正确定位逻辑。

## 图灵入库流程固化成功（大王截图后实机验证 + 固化 xiaowu_agent.py）
- 大王发两张截图演示正确入库流程：① 大货tab下展开筛选区→「关联单据」框填 MCG2026070700039→搜命中1行→点入库；② 弹窗「入库数量：11 / 物料货览」→点确认。大王明确"不用切到大货页面，就是版料的"。
- **关键突破**：之前脚本找不到「关联单据」框，根因是用了**顶部全局搜索框**（只绑 MRK 列）。正确的「关联单据」输入框在**展开的筛选区内部**（`.ant-form-item` 其 label 含"关联单据"）。真正「展开」按钮是 `class="expand"` 的元素（之前匹配到了"搜索重置展开"父容器，点了个寂寞）。
- 诊断 `diag_mcg_tab.py` 定位单号归属：
  - `MCG2026070700039`（截图单号）：**两 tab 都搜不到**（版料0行、大货搜后0行）→ 已不在列表（可能已处理）。
  - `MCG202607070008`（7-7 大王指定单）：**版料tab 搜到1行，大货tab搜不到** → 印证大王"就在版料"的判断正确。
- **实机入库成功**：`do_inbound_v2.py` 执 MCG202607070008 全流程 → 切版料tab→展开→填关联单据框→搜(命中1行)→点入库(dist=0精确匹配)→弹窗确认→系统消息「操作成功」、状态变「已完成」✅。
- 过程中修复 `JS_CLICK_INBOUND_ROW` 笔误 `einnerText`→`e.innerText`（之前每次都 ReferenceError 卡死）。
- **已固化到 `xiaowu_agent.py`**：`process_inbound_order(page, order_no)` 重写为验证流程（切版料tab→.expand展开→关联单据框填MCG→搜→校验命中→点入库→确认弹窗→验证状态）。同步替换了 JS 常量块（JS_CLICK_TAB / JS_CLICK_EXPAND / JS_FILL_RELATION_DOC / JS_CLICK_SEARCH / JS_VERIFY_HIT / JS_CLICK_INBOUND_ROW / JS_READ_MODAL / JS_CLICK_MODAL_CONFIRM / JS_FINAL_CHECK）。删除废弃的翻页扫描逻辑（find_inbound_row）。语法检查通过，dry-run 正常。
- CDP 段错误仍偶发（Exit 139/segfault，Python 层不可 catch）；封装 `run_inbound_safe.py`（外层 Python subprocess 捕获子进程崩溃码，自动重试最多6次）解决单次崩溃丢进度问题。
- 批量入库用法：`python xiaowu_agent.py inbound --exec`（从任务表读"待执行"单号）或 `--order-nos MCGxxx`（手动指定）。默认 dry-run。
- 已同步更新 `D:\ai共享盘\MyBrain\团队\小吴.md` 入库职责/脚本表/inbound 子命令说明（关联单据筛选框流程 + 顶部全局框只认MRK的警告）。

## 入库根因修复：antd 受控表单必须用原生 fill（MCG2026070700039）
- `MCG2026070700039` 大王**手动**完成入库（截图：版料tab 高亮 + 入库类型"版料对款采购"，搜到 MRG202607080000020 关联，状态"已提交"→入库成功）。
- **根因定位**：之前 `xiaowu_agent.py` 的 `process_inbound_order` 用 JS `setter` 改 `input.value` 填「关联单据」框 → 图灵是 antd 受控表单，React Form state 不更新 → 抓包发现搜索 POST 参数误写成 `document=MCG...`（属"入库申请单"MRK 字段），服务端按 MRK 搜自然 0 行。这正是 039 自动搜 0 行的真正原因（非 tab 问题，非 CDP 问题）。
- **修复**：`process_inbound_order` 第③④步改 Playwright **原生 `fill`**：定位器 `.ant-form-item:has(.ant-form-item-label:has-text("关联单据")) input`，填完用 `input_value()` 校验确为单号；搜索按钮改点 `.ant-form` 内的「搜索」按钮（避免误点顶部全局搜索）。已 AST 语法检查通过。
- 冗余诊断脚本（diag_*/listen_search/scroll_inbound/verify_inbound/旧 do_inbound_mcg）已 `mv` 归档至 `D:\报销工作台\03_脚本\_archive\`，主目录仅留 `xiaowu_agent.py` / `do_inbound_v2.py` / `run_inbound_safe.py` / `sync_xiaowu_global_status.py` / `run_xiaowu.bat`。
- ⚠️ 新 `process_inbound_order` 尚未用"活着的待入库 MCG"实机回测（当前 008 已自动成功、039 大王手动完成，列表暂无已知待入库 MCG）；下次出现待入库 MCG 时跑 `inbound --exec` 即验证。

## 大王拍板：MCG202607070003 不回退
- 7-7 误操的 `MCG202607070003`（状态已变"已完成"）大王明确"不用回退"。保持现状，不再追问/处理。

## 入库最终验证成功（MCG202607070041，程序化启动 CDP）
- 大王要求验证修复后入库流程能否跑通。本次**程序化启动 Chrome + CDP**（不再依赖大王手动开 bat）：PowerShell `Start-Process chrome --remote-debugging-port=9222 --user-data-dir=C:\temp\chrome-turing-debug`（先杀占用 9222 旧进程、删 SingletonLock）。
- ⚠️ **CDP 端口探测坑**：Python socket 发 `GET /json/version HTTP/1.0` 连 9222 返回 0 字节（看似端口死），实际是 Chrome 拒了缺 Host 头的 HTTP/1.0 请求。正确探测/连接用 HTTP/1.1+Host 头，或直接让 Playwright `connect_over_cdp` 连（它自带标准 HTTP 客户端）——端口其实在监听（`Get-NetTCPConnection` 可见 `LISTEN 9222`）。修好定位器后跑通。
- 修复 `xiaowu_agent.py` 两处后 `MCG202607070041` 自动入库成功（17:58，状态=已完成）：
  1. **搜索按钮**：原 `.ant-form button:has-text("搜索")` 因按钮文字带空格"搜 索"匹配不到 → 改 JS 评估去空格匹配（`innerText.replace(/\s/g,'')==='搜索'`），优先 form 内。
  2. **关联单据输入框**：原 `.ant-form-item:has(.ant-form-item-label:has-text("关联单据")) input` 定位器异常匹配 23 个 hidden input → 改 JS 评估找 label 含"关联单据"的 form-item 内 input、打 `data-rel-doc="1"` 标记，再 `page.fill('input[data-rel-doc="1"]', 单号)` + 校验 `input_value()`（保留 antd 原生 fill 触发 React onChange 的修复）。
- ✅ 至此修复后的 `process_inbound_order` 已用活着的待入库单实机回测通过，整套流程（切版料tab→.expand展开→关联单据框原生fill→JS去空格点搜索→校验命中→点入库→确认弹窗→验证已完成）稳定可用。以后待入库 MCG 跑 `python xiaowu_agent.py inbound --exec`（或 `--order-nos MCGxxx`）即可。
